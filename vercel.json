{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "buildCommand": "git fetch --unshallow || true && git fetch --tags || true && npm run build",
  "installCommand": "npm install"
}
